
<div class="d-flex align-items-center justify-content-between border z-3 py-3 px-1 sticky-top" id="navbar">
  <% if user_signed_in? %>
    <div class="d-flex align-items-center">

      <div>
        <div class="mx-1" > Hi <%= current_user.first_name.capitalize %> 🏄🏻‍♂️ </div>
        <span class="level level-<%= current_user.surf_level.downcase %>"> <%= current_user.surf_level.capitalize %> </span>
      </div>
    </div>

    <div class="d-flex align-items-center">
      <div class="mx-4 my-2 p-1" id="dp-card" style="display: flex; align-items: center; justify-content: center;">
        <%= link_to alerts_path, style:"cursor: pointer; font-size: 2em;" do %>
          <i class="fa-solid fa-bell" id="dp-bell"></i>
        <% end %>
      </div>

      <div class="nav-item dropdown">
        <div class="nav-link" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          <i class="fa-solid fa-water" style="padding: 10px; border-radius: 15px; font-size: 2em; display: flex; align-items: center; justify-content: center;" id="dp"></i>
        </div>
        <ul class="dropdown-menu">
          <li><%= link_to "My feed", my_feed_favourites_path, class: "dropdown-item" %></li>
          <li><%= link_to "Surfspots", surfspots_path, class: "dropdown-item" %></li>
          <li><%= link_to "My Spots", favourites_path, class: "dropdown-item" %></li>
          <li><hr class="dropdown-divider"></li>
          <li><%= link_to "Edit Profile", edit_user_registration_path, class: "dropdown-item" %></li>
          <li><%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: 'dropdown-item' %></li>
        </ul>
      </div>

    </div>
  <% else %>
    <div class="d-flex align-items-center justify-content-between w-100">
      <%= link_to root_path do %>
      <%= image_tag "SurfBuddy_logo.png" %>
      <% end %>
      <%= link_to "Sign in", new_user_session_path, class: "button" %>
    </div>
  <% end %>
</div>
